{
  "name": "reiven-io",
  "version": "0.2.0",
  "description": "Cloudflare-native encrypted file sharing PoC",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 execute reiven-io-db --file=./migrations/001_init.sql --local && wrangler d1 execute reiven-io-db --file=./migrations/002_receiver_delete.sql --local",
    "db:migrate:remote": "wrangler d1 execute reiven-io-db --file=./migrations/001_init.sql --remote && wrangler d1 execute reiven-io-db --file=./migrations/002_receiver_delete.sql --remote"
  },
  "devDependencies": {
    "wrangler": "^4.65.0"
  }
}
